{% extends 'base.html.twig' %}
{% block body %}
    {% if app.user %}
         {% if app.user.title=='administrator' %}
             {% include('security/usernavbar.html.twig') %}
         {% else %}
             {% include('college/usernavbar.html.twig') %}
        {% endif %}
          <div class="page-wrapper">

				<!-- Page Content -->
                <div class="content container-fluid">

					<!-- Page Header -->
					<div class="page-header" style="padding-top: 20px;">
						<div class="row">
							<div class="col">
								<h3 class="page-title">Update Promotion</h3>
								<ul class="breadcrumb">
									<li class="breadcrumb-item">Dashboard</li>
									<li class="breadcrumb-item active">Update Promotion</li>
								</ul>
							</div>
						</div>
					</div>
					<!-- /Page Header -->
                    <!-- Edit Promotion Modal -->
					 <div class="row">
                        <div class="col-md-12 col-sm-12 col-xs-12">
                            <div class="card">
                                    <div class="card-body">
                                      {{ form_start(form) }}
									<div class="form-group">
                                       {{ form_row(form.promofrom) }}
									</div>
									<div class="form-group">
										{{ form_row(form.promoto) }}
									</div>
									<div class="form-group">
										{{ form_row(form.promodate) }}
										</select>
									</div>
									<div class="submit-section">
                                        {{ form_row(form.save) }}
									</div>
								 {{ form_end(form) }}
							</div>
						</div>
                        </div>
                     </div>
				    <!-- /Edit Promotion Modal -->
                </div>
				<!-- /Page Content -->
          </div>
  {% endif %}
{% endblock %}